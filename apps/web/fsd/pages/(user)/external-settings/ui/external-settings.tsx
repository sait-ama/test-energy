'use client';

import { TransferBookmark } from '~features/transfer-bookmark/ui/transfer-bookmark';
import { Container } from '~shared/ui/container';

export const ExternalSettings = () => (
  <Container slim>
    <TransferBookmark />
  </Container>
);
