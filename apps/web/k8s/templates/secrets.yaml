{{- if .Values.secretEnv }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nextjs-monorepo.fullname" . }}-secret
  labels:
    {{- include "nextjs-monorepo.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}